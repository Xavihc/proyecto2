import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rechnyks.splashAndIconApp',
  appName: 'gasApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
